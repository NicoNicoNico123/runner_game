import { defineConfig } from 'vite'

export default defineConfig({
  // Set this to your repository name if deploying to https://<USERNAME>.github.io/<REPO>/
  // For example: base: '/runner_game/',
  base: './', // Use relative paths to make it work in most cases
})

